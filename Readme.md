# PythonCICD

PythonCICD is a small FastAPI application demonstrating a containerized CI/CD
workflow with Poetry, Docker Compose, GitHub Actions, Docker Hub, and Kubernetes.
Docker Compose supports local development; a Kustomize configuration deploys
staging to a single-node k3s cluster.

## Requirements

For native development:

- Python 3.11 or newer (below Python 4)
- Poetry 1.8.2

For containerized development:

- Docker with the Compose plugin

For staging:

- An Ubuntu VM with k3s, including its bundled Traefik and metrics-server
- A VM network address reachable from the Windows host
- `kubectl` access through `k3s kubectl` or a configured kubeconfig

## Native development

Install dependencies and start the API:

```shell
poetry install --no-interaction --no-root
poetry run python -m uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload
```

The API is available at `http://localhost:8000`, with interactive documentation
at `http://localhost:8000/docs`.

## Docker development

Validate and start the merged development configuration:

```shell
docker compose -f compose.yaml -f compose.dev.yaml config
docker compose -f compose.yaml -f compose.dev.yaml up --build
```

The API is available at `http://localhost:5051`. The development overlay
bind-mounts `src/` and `public/` and enables Uvicorn reload.

Stop the application with:

```shell
docker compose -f compose.yaml -f compose.dev.yaml down
```

## Docker image validation

The default build creates a production image containing runtime dependencies only:

```shell
docker build --check .
docker build --tag python-cicd:local .
docker run --rm --publish 8000:8000 python-cicd:local
```

Poetry and development tools such as pytest remain outside the production image.
Build and test the development variant with:

```shell
docker build --build-arg BUILD_ENVIRONMENT=development --tag python-cicd:development .
docker run --rm python-cicd:development python -m pytest -v
```

The builder and runtime stages are pinned to matching Python 3.11.15 Bookworm
variants. Update both `FROM` instructions together when upgrading Python.

## API endpoints

| Method | Path | Purpose |
| --- | --- | --- |
| `GET` | `/` | Application name, version, message, and environment |
| `GET` | `/health/startup` | Confirms application startup completed |
| `GET` | `/health/live` | Confirms the process can serve requests |
| `GET` | `/health/ready` | Confirms the application can receive traffic |
| `GET` | `/items/{item_id}` | Example path and optional `q` query parameter |
| `GET` | `/public/demo.txt` | Static demonstration file |
| `GET` | `/docs` | Swagger UI generated by FastAPI |

`/health/ready` returns HTTP 503 whenever the application lifecycle state is not
ready. The three probe routes are intentionally independent so future startup or
dependency checks can evolve without changing the other probe contracts.

## Configuration

- `APP_ENV` controls the environment reported by `/`. It defaults to `UNKNOWN`,
  is `development` under Docker Compose, and is `staging` in the k3s overlay.
- `APP_TAG` selects the Docker image tag in Compose and defaults to `latest`.
- `BUILD_ENVIRONMENT` controls whether development dependencies enter the image.

No `.env` file, persistent volume, or database configuration is required.

## Tests and quality checks

```shell
poetry run python -m pytest -v
poetry run ruff check .
poetry run ruff format --check .
```

Render the Kubernetes configurations before applying them:

```shell
kubectl kustomize k8s/base
kubectl kustomize k8s/overlays/staging
```

## CI and image publishing

Pushes to the `github-actions-cd-docker` branch, except documentation-only
changes, trigger GitHub Actions. The workflow:

1. Runs tests, Ruff, Dockerfile checks, image tests, and all three probe smoke tests.
2. Renders and offline schema-validates the base and staging configurations.
3. Publishes `rezwanul7/python-cicd:latest` and the immutable
   `rezwanul7/python-cicd:sha-<full-git-sha>` image.

Configure these GitHub Actions secrets:

- `DOCKERHUB_USERNAME`
- `DOCKERHUB_ACCESS_TOKEN`

The local VM is intentionally deployed manually and does not need to be exposed
to GitHub-hosted runners.

## k3s staging deployment

Install k3s on the Ubuntu VM with its default Traefik and metrics-server enabled.
Do not use the `--disable=traefik` or `--disable=metrics-server` install options.
Confirm the cluster components are ready:

```shell
sudo k3s kubectl get nodes
sudo k3s kubectl get pods --all-namespaces
```

Use an external Hyper-V switch or another network configuration that makes the
VM IP reachable from Windows. Find the address with `hostname -I`. If UFW is
enabled, allow HTTP traffic:

```shell
sudo ufw allow 80/tcp
```

As Administrator, add the following entry to the Windows hosts file at
`C:\Windows\System32\drivers\etc\hosts`:

```text
<VM_IP> python-cicd.test
```

After CI publishes an image, replace `sha-replace-with-full-git-sha` in
`k8s/overlays/staging/kustomization.yaml` with its immutable `sha-<full-git-sha>`
tag and commit that manifest change. From a repository checkout on the VM, deploy
and inspect staging:

```shell
sudo k3s kubectl apply -k k8s/overlays/staging
sudo k3s kubectl rollout status deployment/python-cicd -n python-cicd-staging --timeout=120s
sudo k3s kubectl get pods,service,ingress,hpa,pdb -n python-cicd-staging
sudo k3s kubectl describe hpa python-cicd -n python-cicd-staging
curl --fail --header 'Host: python-cicd.test' http://<VM_IP>/health/ready
```

From Windows, browse to `http://python-cicd.test`. Kubernetes starts two replicas,
performs zero-unavailable rolling updates, and can scale between two and five pods
at 70% average CPU utilization. The disruption budget protects one replica during
voluntary disruptions, but a single-node cluster cannot provide node-level high
availability.

To observe pod replacement safely:

```shell
sudo k3s kubectl delete pod -n python-cicd-staging -l app.kubernetes.io/name=python-cicd
sudo k3s kubectl get pods -n python-cicd-staging --watch
```
